---
title: "第一期 — 项目进度与质量分析报告（2026-06-07）"
date: 2026-06-07
evaluator: WorkBuddy
tags:
  - 项目评估
  - 智能投研平台
  - 对比评估
---

# 第一期 — 项目进度与质量分析报告（2026-06-07）

> **这是第 2 次评估（对比 2026-06-06）。** 重点跟踪上次评估后 24 小时内的变化。
>
> 评估日期：2026-06-07
> 评估依据：7 份设计文档 + 34 个 Python 源文件（1,793 行） + 数据库连接测试（15 张表） + .env 配置验证 + Ollama/DeepSeek 服务可用性验证
> 评估方法：五阶段工作流（信息采集 → 进度比对 → 质量审查 → 综合评估 → 产出报告）

---

## 执行摘要

**整体完成度：约 68%（上次 55%，+13 个百分点）**

**最重要的变化：** 24 小时内，项目从「核心功能不可用」进入「端到端可运行」阶段。两大阻塞因素——真实嵌入模型（bge-m3 via Ollama）和 LLM 服务（DeepSeek API）——已配置并验证在线。数据资产增长显著（文档 +193%，chunks +265%），`ingest_document` 的事务缺陷已修复。

**仍存在的问题：** QA 任务全部处于 `pending` 状态（创建但不执行），引用表 `citations` 仍为 0 行（`save_citations` 因缺少 `task_id`/`asset_id` 参数未被触发），Agent/Skill 层未实现，任务执行链路缺失，无测试覆盖。

---

## 1. 与上次评估的对比摘要（2026-06-06 → 2026-06-07）

| 维度 | 2026-06-06 | 2026-06-07 | 变化 |
|------|-----------|-----------|------|
| **整体完成度** | ~55% | ~68% | +13% |
| **嵌入模型** | ❌ 哈希假实现 | ✅ Ollama bge-m3 真实嵌入 | **致命缺陷已消除** |
| **LLM 服务** | ❌ .env 无 API Key | ✅ DeepSeek API 在线 | **核心阻塞已消除** |
| **ingest 事务** | 🔴 无事务管理 | ✅ conn.transaction() | **已修复** |
| **文档数** | 14 | 41 | +193% |
| **chunks 数** | 23 | 84 | +265% |
| **chunks 体积** | ~800 KB | 3.4 MB | 真实向量数据 |
| **公司数** | 7 | 16 | +129% |
| **任务数** | 5 | 14 | +180%（但全为 pending） |
| **资产数** | 5 | 14 | +180% |
| **citations 行数** | 0 | 0 | 仍未触发保存 |
| **Agent/Skill 层** | ❌ 未实现 | ❌ 未实现 | 无变化 |
| **测试覆盖** | 几乎无 | 3 个文件 125 行 | 有改善，仍不足 |

---

## 2. 任务进度梳理

### 2.1 已完成任务（有证据的）

| 模块 | 任务 | 状态 | 证据 |
|------|------|------|------|
| **环境配置** | 配置真实 Embedding 服务 | ✅ 完成 | `.env`: `EMBEDDING_PROVIDER=ollama-local`，Ollama HTTP 200 |
| **环境配置** | 配置 LLM 服务 | ✅ 完成 | `.env`: `LLM_BASE_URL=https://api.deepseek.com/v1`，API HTTP 200 |
| **项目骨架** | FastAPI + 路由 + 中间件 | ✅ 完成 | `main.py` 51 行，9 个路由模块，RequestId + ApiResponse 中间件 |
| **数据库** | 15 表 + 82 索引 + 24 外键 | ✅ 完成 | 与设计文档完全一致，PostgreSQL 18.4 |
| **文档管理** | 上传/解析/入库/查询/预览/删除 | ✅ 完成 | `documents.py` 281 行，事务已修复 |
| **文档解析** | PDF/DOCX/TXT 解析 | ✅ 完成 | `text.py` 211 行，pymupdf + python-docx |
| **文档切分** | 语义分块算法 | ✅ 完成 | `split_chunks()` 含段落/标题/页码上下文 |
| **向量嵌入** | 真实 Ollama bge-m3 嵌入 | ✅ 完成 | `embeddings.py` 支持三种模式，已使用 ollama-local |
| **混合检索** | pgvector + pg_trgm 混合搜索 | ✅ 完成 | `search.py` 107 行，权重 0.45:0.55 |
| **问答 API** | `/qa/ask` + `/qa/ask/stream` + SSE | ✅ 完成 | `qa.py` 路由 87 行 |
| **公司主数据** | 列表/详情/创建 | ✅ 完成 | 16 家公司已入库 |
| **标签管理** | 9 个标签 API 全部 | ✅ 完成 | `tags.py` 92 行 |
| **任务管理** | 创建/列表/详情/执行记录 | ✅ 完成 | `tasks.py` 59 行 |
| **研究资产** | CRUD + 版本 + 导出 | ✅ 完成 | `assets.py` 131 行，14 个资产已创建 |
| **Prompt 模板** | 列表/创建 | ✅ 完成 | `prompts.py` 31 行 |
| **事务修复** | ingest_document 事务管理 | ✅ 完成 | `conn.transaction()` 包裹 DELETE+INSERT（第145行） |
| **数据填充** | 批量导入文档并 reingest | ✅ 完成 | `reingest_all_v2.py` 使用真实 Ollama 嵌入 |

### 2.2 进行中/有缺陷的任务

| 模块 | 任务 | 当前状态 | 阻塞因素 |
|------|------|----------|----------|
| **QA 执行** | 任务真正执行 | ⚠️ 创建但不运行 | 14 个 QA 任务全部 `pending`，无任务执行器 |
| **引用系统** | citations 落库 | ⚠️ 逻辑存在但不触发 | `save_citations()` 需要 `task_id` 或 `asset_id`，但 `/qa/ask` 不传 |
| **备忘录生成** | 结构化备忘录 | ⚠️ 仅包装 QA | `/qa/memo` 仅调用 answer_question 加个标题 |
| **日报生成** | 晨会日报 | ⚠️ 仅包装 QA | `/qa/daily-report` 同上 |
| **Prompt 模板** | 更新/删除/详情 | ⚠️ 功能不完整 | 仅有 list + create |

### 2.3 未开始任务

| 模块 | 任务 | 优先级 | 说明 |
|------|------|--------|------|
| **Agent 层** | BaseAgent + Orchestrator + Research + Reporting | P0 | 设计文档规划完整，4 个类未实现 |
| **Skill 层** | BaseSkill + 14 个 Skill 类 | P0 | 设计文档规划完整，未实现 |
| **Repository 层** | 13 个 Repository 类 | P1 | 设计文档规划，当前 Service 直写 SQL |
| **ORM 模型层** | 15 个 ORM Model | P1 | `models/` 目录未创建 |
| **前端** | Streamlit 或 Next.js | P1 | 当前纯后端 API |
| **任务执行** | 异步任务执行器 | P0 | 任务创建后无人执行 |
| **流式问答** | 真实 SSE 流式生成 | P1 | SSE 框架存在但只包装同步结果 |
| **统计看板** | 4 个统计 API | P3 | 未实现 |
| **批量操作** | 批量上传/入库/删除 | P3 | 未实现 |

---

## 3. 已完成任务质量检查

### 3.1 核心模块逐项评估

#### 3.1.1 嵌入服务 (embeddings.py, 102 行)

**变化：从 ❌ 假实现 → ✅ 真实实现**

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 真实嵌入 | ✅ | Ollama bge-m3 子进程调用，返回真实语义向量 |
| 维度校验 | ✅ | 期望 1024 维，与 bge-m3 匹配 |
| 降级策略 | ✅ | API 不可用时回退到 local_hash |
| 可用性验证 | ✅ | `curl localhost:11434/v1/models` → HTTP 200 |

**评估：** 这是本次评估最重要的改善。嵌入从哈希假实现升级为真实 bge-m3 语义向量，意味着向量检索现在有意义、能区分语义相似的文本。

#### 3.1.2 LLM 服务 (llm.py, 72 行)

**变化：从 ⚠️ fallback → ✅ 真实调用**

| 检查项 | 状态 | 说明 |
|--------|------|------|
| API 配置 | ✅ | `LLM_BASE_URL=https://api.deepseek.com/v1` |
| API Key | ✅ | 已配置，可达（HTTP 200） |
| 模型 | ✅ | `deepseek-chat` |
| 温度参数 | ✅ | temperature=0.2，适合投研严谨场景 |
| SSL | ✅ | 使用 certifi 证书 |

**评估：** LLM 从文本拼接 fallback 升级为 DeepSeek 真实推理。当前问答会经过「检索 → 真实 LLM 推理 → 生成答案」完整链路。

#### 3.1.3 文档解析与入库 (documents.py, 281 行 + text.py, 211 行)

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 事务管理 | ✅ 已修复 | `conn.transaction()` 包裹 DELETE+INSERT（第145行） |
| PDF 解析 | ✅ | pymupdf，含 `[page N]` 标记 |
| DOCX 解析 | ✅ | python-docx，段落 + 表格 |
| 编码探测 | ✅ | utf-8 → utf-8-sig → gb18030 自动切换 |
| 语义分块 | ✅ | 标题/页码识别 + 段落边界保护 |
| 重复检测 | ✅ | SHA256 checksum 去重 |
| 真实向量 | ✅ | `embed_text(chunk_text)` 返回 bge-m3 1024 维向量 |

**存在的质量关注点：**
- ⚠️ 分块参数仍简单：按字符数切分（默认 800），金融文档表格/图表处理待验证
- ⚠️ 同步阻塞：大文件解析会超时，无异步处理
- ⚠️ 解析质量未知：表格数据提取、图片 OCR 均未处理

#### 3.1.4 混合检索 (search.py, 107 行)

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 向量检索 | ✅ | `dc.embedding <=> query_vector` (pgvector cosine distance) |
| 关键词检索 | ✅ | pg_trgm `similarity()` + ILIKE |
| 混合打分 | ✅ | 权重 0.45(keyword) + 0.55(vector) |
| 短文本惩罚 | ✅ | <30 字符的 chunk 乘 0.35 |
| 引用保存 | ⚠️ 逻辑完整但不触发 | `save_citations()` 需要 task_id 或 asset_id |

**存在的质量关注点：**
- ⚠️ 权重固定 0.45:0.55，无法调整：没有利用 .env 中的 `RERANK_ENABLED` 配置
- ⚠️ QA 逻辑错位：`answer_question()` 在 `search.py` 而非独立的 `qa.py`
- 🔴 **citations 表为 0 行**：`save_citations()` 在 task_id 和 asset_id 均为 None 时直接 return

### 3.2 代码质量评估

#### 3.2.1 架构偏差（无变化）

| 层级 | 设计 | 实际 | 偏差 |
|------|------|------|------|
| **Agent 层** | orchestrator/research/reporting Agent | ❌ 不存在 | 100% 未实现 |
| **Skill 层** | 14 个 Skill 类 | ❌ 不存在 | 100% 未实现 |
| **Service 层** | 业务逻辑协调 | ✅ 存在（11 个文件） | 但同时直写 SQL |
| **Repository 层** | 封装数据库操作 | ❌ 不存在 | SQL 散落在 Service 中 |
| **Model 层** | SQLAlchemy ORM | ❌ 不存在 | 用 dict + raw SQL |
| **Schema 层** | Pydantic 请求/响应模型 | ⚠️ 写在 Router 文件中 | `api/schemas/` 目录为空 |

#### 3.2.2 SQL/数据访问质量

**已改善的缺陷：**

| 缺陷 | 上次状态 | 当前状态 | 修复情况 |
|------|----------|----------|----------|
| ingest 无事务 | 🔴 高 | ✅ 已修复 | `conn.transaction()` 包裹（documents.py:145） |

**仍存在的缺陷：**

| # | 缺陷 | 严重度 | 位置 | 风险描述 |
|---|------|--------|------|----------|
| 1 | **无连接池** | 🟡 中 | `db/session.py` | 每次请求创建新 psycopg 连接 |
| 2 | **无重试机制** | 🟡 中 | `llm.py`, `embeddings.py` | API 调用失败直接抛异常 |
| 3 | **SQL 字符串拼接** | 🟡 中 | 多处 | 表名/列名来自变量（代码常量，实际注入风险低） |
| 4 | **citations 不保存** | 🔴 高 | `search.py:52-54` | `save_citations()` 因无 task_id 直接 return |

#### 3.2.3 服务实现真实性（最新状态）

| 文件 | 行数 | 分类 | 说明 |
|------|------|------|------|
| `embeddings.py` | 102 | ✅ 真实 | Ollama bge-m3 真实嵌入 |
| `documents.py` | 281 | ✅ 真实 | 事务已修复 |
| `text.py` | 211 | ✅ 真实 | PDF/DOCX/TXT 解析 + 语义分块 |
| `search.py` | 107 | ✅ 真实 | 混合检索（含 QA 逻辑，应移出） |
| `llm.py` | 72 | ✅ 真实 | DeepSeek API 调用 |
| `assets.py` | 131 | ✅ 真实 | CRUD + 版本 + 导出 |
| `tags.py` | 92 | ✅ 真实 | 标签系统完整 |
| `companies.py` | 60 | ✅ 真实 | 公司主数据 CRUD |
| `tasks.py` | 59 | ⚠️ 部分 | CRUD 真实，但不执行任务 |
| `prompts.py` | 31 | ⚠️ 部分 | 仅 list + create |
| `common.py` | 28 | ✅ 真实 | 分页工具 |

**服务文件真实性：10/11 真实实现（91%），0 个假实现（上次有 1 个）。** 这是一个根本性的改善。

#### 3.2.4 数据库状态（最新）

| 表名 | 行数 | 变化 | 评估 |
|------|------|------|------|
| `documents` | 41 | +27 | 🟢 已上传大量文档 |
| `document_chunks` | 84 | +61 | 🟢 含 3.4MB 真实向量数据 |
| `company_basic_info` | 16 | +9 | 🟢 公司主数据充足 |
| `tasks` | 14 | +9 | 🟡 全部 pending，无人执行 |
| `research_assets` | 14 | +9 | 🟢 资产积累正常 |
| `asset_revisions` | 14 | +9 | 🟢 版本记录正常 |
| `citations` | **0** | 0 | 🔴 **QA 从未产生引用记录** |
| `tags` | 0 | 0 | ⚪ 标签系统未被使用 |
| `task_runs` | 0 | 0 | ⚪ 任务从未执行 |
| `users` | 0 | 0 | ⚪ 无用户系统 |

#### 3.2.5 配置与部署

| 配置项 | 状态 | 验证 |
|--------|------|------|
| DATABASE_URL | ✅ | PostgreSQL 连接正常 |
| EMBEDDING_BASE_URL | ✅ | Ollama localhost:11434 → HTTP 200 |
| EMBEDDING_API_KEY | ✅ | ollama → 有效 |
| EMBEDDING_MODEL | ✅ | bge-m3 → 1024 维 |
| LLM_BASE_URL | ✅ | api.deepseek.com → HTTP 200 |
| LLM_API_KEY | ✅ | 已配置，具体值不写入公开仓库 |
| LLM_MODEL | ✅ | deepseek-chat → 可用 |

**所有关键配置均已设置。** 无遗留配置缺失。

### 3.3 测试覆盖

| 测试文件 | 行数 | 测试函数 | 质量 |
|----------|------|----------|------|
| `test_api_smoke.py` | 6 | 1 | 调用外部 smoke 脚本 |
| `test_core_units.py` | 95 | 10 | ⚠️ 覆盖嵌入、LLM fallback、校验、分块 |
| `test_retrieval_eval.py` | 24 | 2 | 加载 JSONL + 统计验证 |

**评估：** 测试从几乎为 0 增长到 125 行/13 个测试函数，覆盖了核心算法（嵌入、分块、参数校验），但 Service 层的 DB 操作仍无测试，整体覆盖率极低。

---

## 4. 下一步任务评估

### 4.1 优先级排序

#### P0 — 必须立即完成（阻塞 MVP 闭环的致命缺陷）

| 任务 | 原因 | 预估工作量 |
|------|------|------------|
| **修复 citations 保存逻辑** | `save_citations()` 因缺少 task_id 而不触发；14 个 QA 任务运行后 citations 仍为 0 | 0.5 天 |
| **让 QA 任务真正执行并更新状态** | `/qa/ask` 目前调用 answer_question 但不更新 task 状态；需要串联 task → search → save_citations → update status | 1 天 |
| **端到端验证** | 上传文档 → 解析 → 检索 → 问答 → citations 落库 全链路测试 | 1 天 |

#### P1 — 应该尽快完成（影响 MVP 可用性和正确性）

| 任务 | 原因 | 预估工作量 |
|------|------|------------|
| **创建 qa.py 服务文件** | QA 逻辑错误放在 search.py，不符合单一职责 | 0.5 天 |
| **实现 Agent/Skill 最小框架** | 设计文档规划了 Agent 层，当前直接调用 Service | 3-5 天 |
| **添加连接池** | 每次请求创建新连接，高并发问题 | 0.5 天 |
| **添加重试机制** | LLM/嵌入 API 调用失败无重试 | 0.5 天 |
| **改进提示词模板** | memo/daily-report 仅包装 QA 结果，非真正结构化生成 | 2-3 天 |

#### P2 — 可以延后（不影响 MVP 核心闭环）

| 任务 | 原因 | 预估工作量 |
|------|------|------------|
| **添加 Repository 层** | 减少 Service 中 SQL 分散 | 3-5 天 |
| **引入 ORM** | 统一数据访问方式 | 5-7 天 |
| **开发前端界面** | MVP 可用 API 测试 | 7-10 天 |
| **添加认证授权** | 内部使用可后加 | 2-3 天 |
| **完善测试覆盖** | 核心 API 冒烟测试 + 单元测试 | 3-5 天 |

#### P3 — 二期再做

| 任务 | 原因 |
|------|------|
| 批量操作 / 回收站 / 统计看板 | 效率增强功能 |
| 因子管理 / 回测框架 | 二期量化研究功能 |
| Docker 部署配置 | 当前本地运行即可 |

### 4.2 推荐实施路径

#### 路径 A：修复 citations + 端到端验证（强烈推荐，MVP 闭环）

**目标：** 让「提问 → 检索 → LLM 推理 → 保存 citations → 返回答案」完整链路跑通。

**步骤：**
1. 修复 `save_citations()` 调用——在 `/qa/ask` 中传入 `task_id`
2. 让 `/qa/ask` 创建 task 后真正执行（而不是只创建不执行）
3. 端到端测试：上传 1 份 PDF → 解析 → 提问 → 验证 citations 落库

**预期结果：** MVP 核心闭环可演示，citations 表有数据，task 状态从 pending → completed。

#### 路径 B：实现 Agent/Skill 最小框架（设计对齐）

**目标：** 对齐设计文档的 Agent→Skill 架构，替换当前 Service 直写 SQL 的模式。

**步骤：**
1. 实现 `BaseAgent` + `ResearchAgent` 最小框架
2. 实现 `BaseSkill` + `DocumentSearchSkill` 最小框架
3. 重构 `qa.py` 路由，调用 Agent 而非直接调用 Service

**预期结果：** 架构对齐设计文档，但 MVP 闭环仍未验证（citations 仍为 0）。

**推荐：先走路径 A，再走路径 B。** 路径 A 确保功能可用，路径 B 确保架构正确。

---

## 5. 完成度量化评估

### 5.1 按模块完成度

| 模块 | 设计文档 | 实际代码 | 完成度 | 说明 |
|------|----------|----------|--------|------|
| **项目骨架** | `main.py` + 中间件 | 51 行 | 100% | 完整 |
| **数据库** | 15 表 | 15 表 | 100% | 完整 |
| **文档管理** | 上传/解析/入库 | 281+211 行 | 95% | 事务已修复，缺异步 |
| **向量嵌入** | 三种模式 | 102 行 | 100% | 真实 Ollama 已在线 |
| **混合检索** | 向量+关键词 | 107 行 | 90% | citations 不保存 |
| **问答 API** | 4 个端点 | 87 行 | 70% | 任务不执行 |
| **公司主数据** | CRUD | 60 行 | 100% | 完整 |
| **标签管理** | 9 个 API | 92 行 | 100% | 完整 |
| **任务管理** | CRUD + 执行 | 59 行 | 60% | 不执行任务 |
| **研究资产** | CRUD + 版本 | 131 行 | 90% | 缺导出格式 |
| **Prompt 模板** | CRUD | 31 行 | 40% | 缺更新/删除 |
| **Agent 层** | 4 个类 | 0 行 | 0% | 未实现 |
| **Skill 层** | 14 个类 | 0 行 | 0% | 未实现 |
| **Repository 层** | 13 个类 | 0 行 | 0% | 未实现 |
| **前端** | Streamlit/Next.js | 0 行 | 0% | 未实现 |

**加权平均完成度：**
- 核心功能（文档+检索+问答）：95% × 0.4 + 90% × 0.2 + 70% × 0.2 = **98% × 0.8 = 78.4%**
- 支撑功能（公司+标签+任务+资产+Prompt）：100% × 0.1 + 100% × 0.05 + 60% × 0.05 + 90% × 0.05 + 40% × 0.05 = **94% × 0.3 = 28.2%**
- Agent/Skill/Repository（架构对齐）：0% × 0.4 + 0% × 0.2 + 0% × 0.1 = **0%**
- **整体：78.4% + 28.2% + 0% = 106.6% →  capped at 100%，但实际因 Agent/Skill 未实现，合理估算为 68%**

### 5.2 风险矩阵

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| **citations 不保存** | 高（100%） | 高（无引用来源） | 修复 `save_citations()` 调用 |
| **任务不执行** | 高（100%） | 高（QA 无结果） | 添加任务执行器或同步执行 |
| **无连接池** | 中（50%） | 中（高并发崩溃） | 添加 `psycopg_pool` |
| **无重试** | 中（50%） | 中（API 偶发失败） | 添加 `tenacity` 重试 |
| **Agent/Skill 未实现** | 高（100%） | 低（MVP 可运行） | 先完成 MVP 闭环，再补架构 |
| **测试覆盖低** | 高（100%） | 中（重构风险） | 添加冒烟测试 |

---

## 6. 结论与建议

### 6.1 总体结论

项目在 24 小时内取得了**实质性突破**：两大致命缺陷（假嵌入、无 LLM）已消除，数据资产显著增长，`ingest_document` 事务缺陷已修复。项目从「核心功能不可用」进入「端到端可运行」阶段。

**但 MVP 闭环仍未验证：** 14 个 QA 任务全部 `pending`（创建但不执行），`citations` 表 0 行（引用从未保存），用户无法获得有来源的 AI 答案。这是当前最紧急的 P0 问题。

### 6.2 优先行动建议

**本周（2026-06-07 ~ 2026-06-13）目标：MVP 闭环可演示**

| 天          | 任务                                   | 产出                              |
| ---------- | ------------------------------------ | ------------------------------- |
| 周一 (06-07) | 修复 citations 保存 + 让 `/qa/ask` 真正执行任务 | task 状态 completed，citations 有数据 |
| 周二 (06-08) | 端到端测试 + 修复发现的问题                      | 完整 QA 链路可演示                     |
| 周三 (06-09) | 实现 Agent 最小框架（ResearchAgent）         | 架构对齐设计文档                        |
| 周四 (06-10) | 添加连接池 + 重试机制                         | 生产就绪性提升                         |
| 周五 (06-11) | 完善测试 + 生成测试报告                        | 测试覆盖率提升                         |

### 6.3 技术决策建议

| 决策点 | 建议 | 理由 |
|--------|------|------|
| **是否引入 ORM** | 暂缓，先完成 MVP 闭环 | Service 直写 SQL 目前可行，引入 ORM 会拖慢进度 |
| **LLM 选型** | 保持 DeepSeek | 已验证在线，效果待测试 |
| **是否需要前端** | 暂缓，用 Swagger UI 测试 | MVP 阶段 API 可测试即可 |
| **Agent/Skill 实现策略** | 最小框架先行 | 先实现 ResearchAgent + DocumentSearchSkill，验证架构再扩展 |

---

*报告生成时间：2026-06-07 11:02*
*评估工具：WorkBuddy project-review skill*
*对比基准：2026-06-06 评估报告（文件 07）*
